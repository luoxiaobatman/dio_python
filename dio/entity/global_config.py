from dio.share import Entity


class EntityGlobalConfig(Entity):
    # ---------------------------- Base 字段定义 START ----------------------------
    id: int
    # ---------------------------- Base 字段定义 END ----------------------------

    # CACHE_FIELDS = ['uid', 'picture_cost_index', 'video_cost_index', 'picture_forbidden', 'video_forbidden']
